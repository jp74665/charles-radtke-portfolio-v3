import { createContext, useState, useContext } from 'react';

// Optional: A simple context could be used for the Work overlay, 
// but since the instruction implies handling it in the Work component, 
// I will keep it there. Creating an empty placeholder if needed.
